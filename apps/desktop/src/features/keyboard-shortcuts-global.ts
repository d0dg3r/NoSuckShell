/**
 * Phase 2 (optional): register OS-global shortcuts via Tauri, e.g. `tauri-plugin-global-shortcut`,
 * to focus the app or run commands while another app is focused. Not wired yet — window must be
 * focused for shortcuts handled in `useAppKeyboardShortcutEngine`.
 */
export const KEYBOARD_SHORTCUTS_GLOBAL_PHASE = 2;
